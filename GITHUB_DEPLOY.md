# 🚀 Установка с GitHub

**Для сервера**: TWauto (52.68.160.171, Tokyo)

---

## 📋 Преимущества установки через GitHub

- ✅ Легко обновлять (`git pull`)
- ✅ Версионирование
- ✅ Backup кода
- ✅ Простая установка одной командой

---

## 🔧 Установка на Сервер (с GitHub)

### Шаг 1: Подключиться к серверу

```bash
ssh root@52.68.160.171
```

---

### Шаг 2: Добавить Swap (ОБЯЗАТЕЛЬНО для 512 MB RAM!)

```bash
# Создать swap 1 GB
sudo fallocate -l 1G /swapfile
sudo chmod 600 /swapfile
sudo mkswap /swapfile
sudo swapon /swapfile
echo '/swapfile none swap sw 0 0' | sudo tee -a /etc/fstab

# Проверить
free -h
```

**Ожидается**: `Swap: 1.0Gi` в выводе

---

### Шаг 3: Клонировать Репозиторий

```bash
# Установить git (если не установлен)
sudo apt update
sudo apt install -y git

# Клонировать проект
git clone https://github.com/ВАШ_USERNAME/tw-auto-executor.git
cd tw-auto-executor

# Проверить что клонировалось
ls -la
```

---

### Шаг 4: Запустить Автоматический Установщик

```bash
chmod +x install.sh
./install.sh
```

**Установщик автоматически**:
- ✅ Установит Node.js 20.x
- ✅ Установит PM2
- ✅ Установит зависимости (`npm install`)
- ✅ Соберет проект (`npm run build`)
- ✅ Создаст `.env` из `env.example`

**Время**: 3-5 минут

---

### Шаг 5: Настроить .env

```bash
# Сгенерировать сильный пароль
openssl rand -base64 32

# Редактировать .env
nano .env
```

**Изменить**:
```bash
# 1. ОБЯЗАТЕЛЬНО: Сменить passphrase
WEBHOOK_PASSPHRASE=ВАШ_СГЕНЕРИРОВАННЫЙ_ПАРОЛЬ

# 2. ОБЯЗАТЕЛЬНО: API ключи
BINANCE_API_KEY=ваш_api_key
BINANCE_SECRET=ваш_secret

# 3. ОБЯЗАТЕЛЬНО: Production mode
NODE_ENV=production

# 4. ОПЦИОНАЛЬНО: Telegram
# TELEGRAM_BOT_TOKEN=...
# TELEGRAM_CHAT_ID=...
```

**Сохранить**: `Ctrl+X` → `Y` → `Enter`

---

### Шаг 6: Настроить Firewall

```bash
sudo ufw allow 22/tcp
sudo ufw allow 3000/tcp
sudo ufw --force enable
sudo ufw status
```

---

### Шаг 7: Запустить Сервис

```bash
# Запустить
pm2 start ecosystem.config.js

# Сохранить конфигурацию
pm2 save

# Настроить автозапуск
pm2 startup
```

**ВАЖНО**: Выполните команду которую покажет `pm2 startup`!

---

### Шаг 8: Проверить Работу

```bash
# Статус
pm2 status

# Логи
pm2 logs tw-auto-executor --lines 50

# Health check
curl http://localhost:3000/health

# Память
pm2 show tw-auto-executor | grep memory
free -h
```

**Ожидается**:
- ✅ Status: `online`
- ✅ Memory: < 300 MB
- ✅ Health: `{"status":"ok"...}`

---

### Шаг 9: Проверить Извне (с вашего Mac)

```bash
curl http://52.68.160.171:3000/health
```

---

## 🔄 Обновление Проекта (в будущем)

```bash
# На сервере
cd ~/tw-auto-executor

# Получить изменения
git pull

# Переустановить зависимости (если package.json изменился)
npm install

# Пересобрать
npm run build

# Перезапустить
pm2 restart tw-auto-executor
```

---

## 🎉 Готово!

Сервис установлен и работает!

**Следующие шаги**:
1. Настроить TradingView Webhook
2. Протестировать с минимальным объемом
3. Мониторить первые 24 часа

---

**Создано**: 22 декабря 2024  
**Для**: TWauto Server (52.68.160.171)

